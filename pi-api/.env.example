# Pi API Server Configuration

# Server Port
PORT=3000

# Node Environment
NODE_ENV=production

# Backend API URL (for online sync)
BACKEND_URL=https://api.yourdomain.com

# Pi Identifier (unique ID for this Raspberry Pi)
PI_ID=pi-001

# Database Path (optional, defaults to ./db/pi-bridge.db)
# DB_PATH=/home/pi/raspi-ios-bridge/pi-api/db/pi-bridge.db

# Screenshots Directory
SCREENSHOTS_DIR=./screenshots

# Auto-sync interval in minutes (0 to disable)
AUTO_SYNC_INTERVAL=30
